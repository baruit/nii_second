# 🎵 SonicCanvas - AI Audio Analysis & Cover Generator

Приложение для анализа аудио и генерации обложек альбомов с использованием Google Gemini через OpenRouter.

## 🚀 Возможности

### 0. 🖼 Галерея
- **Публичная галерея**: смотреть и слушать можно без регистрации
- **Личный CRUD**: добавлять/редактировать/удалять проекты может только автор (или админ)

### 1. 🎨 Эмоциональный анализ аудио (Gemini 2.5 Flash)
Проанализируйте свой трек как настоящий искусствовед! Получите:
- **Транскрипцию** текста/слов из аудио
- **Эмоциональную палитру** (радость, грусть, энергия, меланхолия и т.д.)
- **Вайб и атмосферу** композиции
- **Музыкальный стиль** (жанр, инструменты, темп)
- **Визуальные ассоциации** (образы, цвета, сцены)
- **Целевую аудиторию**
- **Коммерческий потенциал** трека

### 2. 🔥 Генерация обложек (Gemini 3 Pro Image)
Создайте **НЕВЕРОЯТНУЮ** обложку альбома на основе музыкального анализа:
- Визуально захватывающая и запоминающаяся
- Отражает эмоции и вайб трека
- Профессиональное качество
- Современный дизайн
- Квадратный формат 1:1 для стриминговых платформ

## 🛠 Технологический стек

### Backend
- **Node.js** + **TypeScript** + **Express**
- **PostgreSQL** для хранения проектов
- **Логин/пароль** (сессии в БД) + роли `user/admin`
- **OpenRouter API** для AI моделей:
  - `google/gemini-2.5-flash` - транскрипция и эмоциональный анализ
  - `google/gemini-3-pro-image-preview` - генерация обложек
- **Multer** для загрузки файлов

### Frontend
- **React** + **TypeScript** + **Vite**
- **TailwindCSS** для стилизации
- **Axios** для API запросов
- **Lucide React** для иконок

## 📦 Установка и запуск

### Предварительные требования
1. Node.js (v18+)
2. PostgreSQL
3. OpenRouter API ключ ([получить здесь](https://openrouter.ai/keys))

### 1. Установка зависимостей

```bash
# Backend
cd backend
npm install

# Frontend
cd ../frontend
npm install
```

### 2. Настройка базы данных

Запустите PostgreSQL и создайте базу данных:

```bash
# В контейнере или локально
psql -U postgres
CREATE DATABASE audioprojects;
```

### 3. Настройка переменных окружения

Отредактируйте `backend/.env`:

```env
PORT=3000
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_password
POSTGRES_DB=audioprojects
POSTGRES_PORT=5455
POSTGRES_HOST=localhost

# Получите на https://openrouter.ai/keys
OPENROUTER_API_KEY=sk-or-v1-XXXXXXX

# (опционально) срок жизни сессии (Bearer token) в днях
SESSION_TTL_DAYS=30

# (опционально) сид для админа: при старте создаст/повысит пользователя до admin
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change_me

# Cloudflare R2 (публичное хранение аудио/обложек вместо backend/uploads)
# S3 endpoint вида: https://<ACCOUNT_ID>.r2.cloudflarestorage.com
R2_ENDPOINT=https://<ACCOUNT_ID>.r2.cloudflarestorage.com
R2_BUCKET=soniccanvas
R2_ACCESS_KEY_ID=XXXX
R2_SECRET_ACCESS_KEY=XXXX
# Публичный base URL (R2.dev или ваш домен), например: https://pub-xxxx.r2.dev
R2_PUBLIC_BASE_URL=https://pub-xxxx.r2.dev
# (опционально) префикс для неймспейса объектов, например: soniccanvas
R2_PREFIX=soniccanvas
```

Если `R2_*` переменные не заданы — файлы сохраняются локально в `backend/uploads` и раздаются через `/uploads`.
Если `R2_*` заданы — `audio_url`/`cover_url` будут публичными URL из R2.

### 4. Запуск приложения

```bash
# Терминал 1 - Backend
cd backend
npm start

# Терминал 2 - Frontend
cd frontend
npm run dev
```

Приложение будет доступно по адресу: **http://localhost:5173**

## 🚢 Деплой (Railway + Cloudflare R2)

### Backend (Railway)
1. Создайте новый проект в Railway и добавьте сервис из папки `backend`
2. Добавьте PostgreSQL (Railway plugin) и проставьте `POSTGRES_*` переменные в сервис backend
3. Добавьте `OPENROUTER_API_KEY` и `R2_*` переменные (см. выше)
4. Убедитесь, что bucket в R2 включён как public (или привязан custom domain) и `R2_PUBLIC_BASE_URL` ведёт на него

### Frontend
- Если фронт хостится отдельно (Vercel/Netlify), задайте `VITE_API_BASE_URL` (например `https://<your-backend>.up.railway.app/api`) или настройте rewrite `/api -> backend`.

## 🎯 Как использовать

1. **Откройте галерею** — смотреть и слушать можно без регистрации
2. **Войдите/зарегистрируйтесь** — чтобы добавить свой проект
3. **Создайте проект** — нажмите "New Project" и запишите аудио
4. **Анализируйте** — в проекте нажмите "🎵 Анализировать" (доступно автору/админу)
5. **Создайте обложку** — нажмите "🔥 Создать обложку" (доступно автору/админу)

## 📁 Структура проекта

```
nii/
├── backend/
│   ├── server.ts          # Express сервер с API endpoints
│   ├── uploads/           # Загруженные аудиофайлы
│   └── .env              # Переменные окружения
├── frontend/
│   ├── src/
│   │   ├── App.tsx       # Главный компонент
│   │   └── components/
│   │       ├── AudioRecorder.tsx    # Запись аудио
│   │       ├── ProjectList.tsx      # Список проектов
│   │       └── ProjectView.tsx      # Просмотр проекта
│   └── index.html
└── README.md
```

## 🔌 API Endpoints

### Auth
- `POST /api/auth/register` - Регистрация (логин/пароль)
- `POST /api/auth/login` - Вход (логин/пароль)
- `POST /api/auth/logout` - Выход
- `GET /api/auth/me` - Текущий пользователь

### Projects
- `GET /api/projects` - Получить все проекты
- `GET /api/projects/:id` - Получить проект по ID
- `POST /api/projects` - Создать новый проект (только авториз.)
- `PUT /api/projects/:id` - Переименовать проект (только автор/админ)
- `DELETE /api/projects/:id` - Удалить проект (только автор/админ)

### AI Processing
- `POST /api/transcribe/:id` - Анализ аудио с Gemini 2.5 Flash (только автор/админ)
- `POST /api/generate-cover/:id` - Генерация обложки с Gemini 3 Pro Image (только автор/админ)

## 🎨 Промпты

### Промпт для анализа аудио
Система просит Gemini 2.5 Flash:
- Прослушать аудиофайл
- Дать полный анализ как искусствовед и музыкальный критик
- Описать эмоции, вайб, стиль, визуальные ассоциации
- Оценить коммерческий потенциал

### Промпт для обложки
На основе музыкального анализа Gemini 3 Pro Image создает:
- Визуально захватывающую обложку
- Отражающую эмоции и вайб трека
- В профессиональном качестве
- С целью "продать миллион копий"

## 💡 Особенности реализации

- **Audio Input**: Gemini 2.5 Flash принимает аудио в base64 формате
- **Fallback**: Если Gemini не отвечает, используется Pollinations AI для обложек
- **Storage**: обложки скачиваются и сохраняются локально в `backend/uploads/covers`
- **Mock Data**: При ошибках API показываются демо-данные
- **Real-time**: Обновление UI в реальном времени после обработки

## 🔥 Roadmap

- [ ] Поддержка загрузки файлов (не только запись)
- [ ] Экспорт обложек в высоком разрешении
- [ ] История версий обложек
- [ ] Редактирование обложек
- [ ] Поддержка разных форматов аудио
- [ ] Batch processing нескольких треков

## 📝 License

MIT

---

Создано с ❤️ и Google Gemini моделями через OpenRouter 🚀
# nii_second
